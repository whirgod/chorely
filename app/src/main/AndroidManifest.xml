<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runtime permission on API 33+. A silently missing digest is almost always this. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Scheduled work does not survive a reboot; BootReceiver re-derives it from Room. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      No INTERNET permission, deliberately: Chorely works fully offline and adding one is a
      product change, not an implementation detail. See "Never do" in AGENTS.md.

      allowBackup stays true: it is the OS's own mechanism, the app never knows about it,
      and with no export feature it is all that survives a lost phone.
    -->
    <application
        android:name=".ChorelyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chorely">

        <!--
          adjustResize so the IME reports its inset and the editor's fields can move out
          from under the keyboard; the activity is edge-to-edge, and on API 26-29 nothing
          else makes `WindowInsets.ime` report a height.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Chorely"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".reminder.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
          WorkManager's default initializer is removed so Hilt's worker factory is used;
          ChorelyApplication supplies the Configuration instead.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            xmlns:tools="http://schemas.android.com/tools">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
